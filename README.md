# Dimas F. B. Rifai — Portfolio

This repo contains a static, single-page portfolio for **Dimas Fakhri Bahtiar Rifai**.

Files:
- `portfolio-single.html` — single-file version (HTML + CSS + JS + SEO + analytics)
- `resume.pdf` — (replace with your CV file)
- `react-tailwind/` — React + Tailwind version (optional)

---

## Quick local preview

1. Place `portfolio-single.html` and `resume.pdf` in the same folder.
2. Open `portfolio-single.html` in your browser.

If you want a simple static server (recommended for testing forms / Netlify attributes), use:
```bash
# Python 3 simple server
python -m http.server 8000
# visit http://localhost:8000/portfolio-single.html
